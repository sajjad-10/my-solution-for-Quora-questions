import Card from "./Card";

function Game() {
  return (
    <section className="memory-game">
      {/* <Card card={card} onClick={} /> */}
      <h1>SSS</h1>
    </section>
  );
}

export default Game;
